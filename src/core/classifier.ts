import type { TaskCategory } from "./contracts";
export const classifyTask = (message: string): TaskCategory => { const t = message.toLowerCase(); if (/\b(code|bug|typescript|python)\b/.test(t)) return "coding"; if (/\b(calculate|calculator|percent|equation)\b/.test(t)) return "calculation"; if (/\b(math|algebra|geometry)\b/.test(t)) return "mathematics"; if (/\b(latest|today|news|weather|price)\b/.test(t)) return "current_information"; if (/\b(file|document|pdf)\b/.test(t)) return "file_analysis"; if (/\b(reason|analyze|compare)\b/.test(t)) return "reasoning"; return t.trim() ? "general" : "unknown"; };
